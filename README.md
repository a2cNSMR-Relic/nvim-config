# Neovim 設定

このリポジトリは Neovim の設定を管理します。

## セットアップ

`~/.config` 配下に `nvim` ディレクトリ名で clone します。

```sh
git clone <REPO_URL> ~/.config/nvim
```

## フォント

フォントの調整は iTerm2 で行っています。

- 通常フォント: Ricty Regular 16pt
- Non-ASCII フォント: Hack Nerd Font Mono Regular 15pt

